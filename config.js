var config = {
    scheme: 'https',
    host: 'jsonplaceholder.typicode.com',
    path: '',   // to be re-configured per request accordingly to current needs
    method: 'GET',
    limit: '5', // limit of posts to be printed
};


module.exports = config;